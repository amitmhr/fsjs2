let user = "Amit"; // string type variable
let admin = true; // boolean type variable
let token = null; // null type variable
let data; // undefined variable

//displaying value and datatype of the variables
console.log(user, typeof(user));
console.log(admin, typeof(admin));
console.log(token, typeof(token));
console.log(data, typeof(data));
